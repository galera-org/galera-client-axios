{
  "name": "@galera/api-client",
  "version": "0.2.0",
  "private": false,
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "lint:check": "biome check .",
    "lint:fixall": "biome check --write .",
    "generate": "TS_POST_PROCESS_FILE='npx @biomejs/biome format --write' openapi-generator-cli generate -i openapi.json -g typescript-axios --additional-properties=supportsES6=true --additional-properties=useSingleRequestParameter=true --additional-properties=withSeparateModelsAndApi=true --additional-properties=modelPackage=models --additional-properties=apiPackage=api --enable-post-process-file -o ./src"
  },
  "author": "Ondřej Pešek",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5",
    "@openapitools/openapi-generator-cli": "2.38",
    "@types/node": "24",
    "typescript": "6"
  }
}
