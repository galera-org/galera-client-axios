{
  "name": "@galera/api-client",
  "version": "0.3.0-alpha.1",
  "private": false,
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "lint:check": "biome check .",
    "lint:fixall": "biome check --write .",
    "generate": "TS_POST_PROCESS_FILE='npx @biomejs/biome format --write' openapi-generator-cli generate -i openapi.json -g typescript-fetch --additional-properties=supportsES6=true --additional-properties=useSingleRequestParameter=true --additional-properties=modelPropertyNaming=original --enable-post-process-file -o ./src"
  },
  "author": "Ondřej Pešek",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@openapitools/openapi-generator-cli": "2.41.0",
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
